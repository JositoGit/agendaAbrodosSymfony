{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        <div class="container">
        {{ form_widget(form) }}
        </div>
    {{ form_end(form) }}
{% endblock %}

